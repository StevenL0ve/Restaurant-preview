# CoParent — environment configuration (Phase 2)
# Copy to .env.local and fill in. These are PUBLISHABLE values (safe in a client
# app); access is protected by Supabase Row-Level Security. Do NOT put the
# Anthropic key here — that lives only as a Supabase Edge Function secret.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
